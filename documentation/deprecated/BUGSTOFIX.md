1. prompt upload fails with Id contains illegal chars.Retry
2. prompt table created in cosmosnosql db but named platform-config and not "SCENARIONAME-prompts"
3. graph topology still does not load correctly, but graph can be visualized in cosmos data explorer and g.v() works
4. agents initialized without prompts. graph explorer and telemetry initialized without graph query api connection 
