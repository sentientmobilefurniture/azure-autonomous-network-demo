# Refactor: Simplify, Solidify, Harden

## Objective:
1. Remove the old settings wheel and make the scenarios dropdown more prominent. Move the full scenarios list from that old settings menu to a small button next tot he scenarios dropdown. You select the available, and then you view all details, that kind of flow. 
2. Default to empty scenario upon loading the page. Get rid of the transparent loading overlay, it is shit. Have something to notify the user that scenarios are currently being loaded from cosmosDB 
3. Can we have a connection button? You open it and see the connected services in a list, with their types, and connectivity status (If they are not connected they are greyed out). And next to the fabric one you can create a fabric connection to a workspace. 
3. Evaluation of actions, authentication and persistence by user, comparison with basic rag
4. Add CRM scenario
5. s