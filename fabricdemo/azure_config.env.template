# ============================================================================
# Autonomous Network NOC Demo — Configuration Template
# ============================================================================
# Copy this file to azure_config.env and fill in the values.
# This is the SINGLE source of truth for all project configuration.
# Edit azure_config.env, then run 'azd up' — preprovision.sh syncs to Bicep.
# Most values are auto-populated by postprovision.sh after each deployment.
# Fabric values are populated by scripts/fabric/populate_fabric_config.py.
# ============================================================================

# --- Core Azure settings (AUTO: populated by postprovision) ---
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_LOCATION=swedencentral

# --- AI Foundry (AUTO: populated by postprovision) ---
AI_FOUNDRY_NAME=
AI_FOUNDRY_ENDPOINT=
AI_FOUNDRY_PROJECT_NAME=
PROJECT_ENDPOINT=

# --- Model deployments (USER: must match infra/modules/ai-foundry.bicep) ---
MODEL_DEPLOYMENT_NAME=gpt-4.1
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536
GPT_CAPACITY_1K_TPM=300

# --- Azure AI Search (AUTO: populated by postprovision) ---
AI_SEARCH_NAME=
RUNBOOKS_INDEX_NAME=runbooks-index
TICKETS_INDEX_NAME=tickets-index

# --- Azure Storage (AUTO: populated by postprovision) ---
STORAGE_ACCOUNT_NAME=

# --- App / CORS (USER: change for production deployment) ---
CORS_ORIGINS=http://localhost:5173

# --- Graph Backend ---
# Controls which graph database backend is used by graph-query-api
# Options: "fabric-gql" (GQL via Microsoft Fabric), "mock" (static responses)
GRAPH_BACKEND=fabric-gql

# --- graph-query-api (AUTO: populated by postprovision / azd outputs) ---
GRAPH_QUERY_API_URI=
# APP_PRINCIPAL_ID is the Container App managed identity principal ID.
# Used by assign_fabric_role.py to grant Fabric workspace access.
APP_PRINCIPAL_ID=

# --- Cosmos DB NoSQL (AUTO: populated by postprovision) ---
COSMOS_NOSQL_ENDPOINT=

# --- Microsoft Fabric (AUTO: populated by populate_fabric_config.py) ---
FABRIC_WORKSPACE_ID=
FABRIC_WORKSPACE_NAME=TelcoAutonomousNetwork
# --- Fabric Capacity (USER: set admin email to auto-provision via Bicep) ---
# Leave AZURE_FABRIC_ADMIN empty to skip Fabric capacity provisioning in Bicep.
AZURE_FABRIC_ADMIN=
FABRIC_CAPACITY_SKU=F8
FABRIC_CAPACITY_ID=
FABRIC_API_URL=https://api.fabric.microsoft.com/v1
FABRIC_SCOPE=https://api.fabric.microsoft.com/.default
FABRIC_ONTOLOGY_ID=
FABRIC_ONTOLOGY_NAME=NetworkTopologyOntology
# FABRIC_GRAPH_MODEL_ID — auto-discovered at runtime from workspace
FABRIC_LAKEHOUSE_NAME=NetworkTopologyLH
FABRIC_EVENTHOUSE_ID=
FABRIC_EVENTHOUSE_NAME=NetworkTelemetryEH
# FABRIC_KQL_DB_NAME — auto-discovered at runtime from eventhouse
# EVENTHOUSE_QUERY_URI — auto-discovered at runtime from eventhouse
